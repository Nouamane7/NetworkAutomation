export interface AddInterfaceRequest {
    ip_address: string;
    subnet_mask: string;
    interface_name: string;
    username: string;
    password: string;
    port: string;
    connected_to: string;
    interface_neib: string;
}